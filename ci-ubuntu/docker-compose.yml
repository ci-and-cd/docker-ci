
version: '3.6'
services:
  ci-ubuntu:
    build:
      context: .
      dockerfile: Dockerfile
      #args:
      #- build_fileserver=${BUILD_FILESERVER:-http://fileserver.local}
    image: home1oss/ci-ubuntu:18.04
